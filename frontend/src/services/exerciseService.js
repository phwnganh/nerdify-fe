import { SERVER_URI } from "../constants";
import client from "./client";

// export const getExercises = async () => {
//   const response = await client.get(SERVER_URI.EXERCISES_SERVICE);
//   console.log("data: ", response);
  
//   return response.data;
// };
